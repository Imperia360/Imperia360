const PHONE='573229667868';export function createWhatsAppUrl(product=null,baseMessage='Hola IMPERIA 360, necesito ayuda.'){const p=[baseMessage];if(product?.name)p.push(`Producto: ${product.name}`);if(product?.identification?.manufacturerReference)p.push(`Referencia: ${product.identification.manufacturerReference}`);if(product?.identification?.sku)p.push(`SKU: ${product.identification.sku}`);if(product?.slug)p.push(`Enlace: ${product.slug}`);return`https://wa.me/${PHONE}?text=${encodeURIComponent(p.join('\n'))}`}export function whatsappButton(product=null,label='Cotizar por WhatsApp'){const a=document.createElement('a');a.className='btn green';a.target='_blank';a.rel='noopener noreferrer';a.href=createWhatsAppUrl(product);a.textContent=label;return a}
