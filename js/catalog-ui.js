import{productCard}from'./product-card.js';export function renderEmptyState(container,title='Estamos preparando tu catálogo',message='Esta sección se activará cuando existan datos reales.'){if(!container)return;container.hidden=false;container.className='empty';container.replaceChildren();const s=document.createElement('strong');s.textContent=title;container.append(s,document.createTextNode(message))}export function renderLoadingState(container,count=3){if(!container)return;container.hidden=false;container.replaceChildren(...Array.from({length:count},()=>{const e=document.createElement('div');e.className='skeleton';return e}))}export function renderCategories(container,categories=[]){if(!container)return;container.replaceChildren(...categories.map((category,index)=>{const c=document.createElement('article');c.className='category-card';const i=document.createElement('div');i.className='category-icon';i.textContent=String(index+1).padStart(2,'0');const n=document.createElement('div');n.className='num';n.textContent='LÍNEA COMERCIAL';const h=document.createElement('h3');h.textContent=category.name??'';const p=document.createElement('p');p.textContent='Productos, presentaciones y cotización.';const tag=document.createElement('span');tag.className='pill';tag.textContent='Explorar línea';c.append(i,n,h,p,tag);return c}))}export function renderProductResults(container,products=[]){if(!container)return;if(!products.length){renderEmptyState(container);return}container.hidden=false;container.className='grid';container.replaceChildren(...products.map(productCard))}
